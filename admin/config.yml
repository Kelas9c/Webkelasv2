backend:
  name: git-gateway
  branch: main

media_folder: "images"

collections:
  - name: "test"
    label: "Tes"
    folder: "_posts"
    create: true
    fields:
      - { label: "Judul Tes", name: "title", widget: "string" }
